// Test file to verify Amazon Q rule application

function calculateSum(a, b) {
    return a + b;
}

function multiplyNumbers(x, y) {
    return x * y;
}

module.exports = { calculateSum, multiplyNumbers };
